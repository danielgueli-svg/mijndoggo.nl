---
import BaseLayout from "../../layouts/BaseLayout.astro";
import TipCard from "../../components/TipCard.astro";
import { unsplashSrc } from "../../lib/images";
import { getCollection } from "astro:content";

const tips = (await getCollection("tips")).sort(
  (a, b) => a.data.publishedAt.getTime() - b.data.publishedAt.getTime(),
);
---

<BaseLayout
  title="Tips & trucs"
  description="Geen college, wel dingen die je morgen kunt gebruiken — wassen, wandelen, voeding en die eerste weken met een pup — in het Nederlands, zonder stijve brochuretoon."
>
  <div class="mx-auto max-w-6xl px-4 pb-4 pt-10 sm:px-6">
    <p class="text-xs font-extrabold uppercase tracking-widest text-sky-deep">Tips & trucs</p>
    <h1 class="mt-2 font-display text-4xl font-semibold sm:text-5xl">Voor baasjes die het graag leuk houden</h1>
    <p class="mt-3 max-w-2xl text-lg text-muted">
      Geen college, wel dingen die je morgen kunt gebruiken — wassen, wandelen, voeding en
      die eerste weken met een pup — in het Nederlands, zonder stijve brochuretoon. Lees op
      jouw tempo, pak eruit wat past, en laat de rest liggen tot jullie eraan toe zijn.
    </p>
  </div>

  <section class="mx-auto max-w-6xl px-4 py-6 sm:px-6">
    <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-3">
      {
        tips.map((tip) => (
          <TipCard
            href={`/tips/${tip.id}`}
            title={tip.data.title}
            description={tip.data.description}
            category={tip.data.category}
            minutes={tip.data.minutes}
            image={unsplashSrc(tip.data.heroImage, 800, 450)}
            imageAlt={tip.data.heroImageAlt}
          />
        ))
      }
    </div>
  </section>
</BaseLayout>
